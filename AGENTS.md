# Agent Instructions

Before doing optimization work in this project, read `LONG_TERM_OPTIMIZATION_PLAN.md`.

When finishing an optimization pass:

- summarize progress against the plan;
- mention verification performed;
- update `LONG_TERM_OPTIMIZATION_PLAN.md` if status, scope, or next steps changed.

Do not commit or expose private conversation data by default. `talks/`, `analysis/`, `.env`, `.venv/`, and local CodeGraph database files are intentionally ignored.

